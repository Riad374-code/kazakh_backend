# Full local development / ML training dependencies for KazakhAI_ML_Gemini.
# Railway deployment uses requirements-server.txt (torch-free, small image).
# Torch installs a multi-GB wheel - keep it OUT of the deploy image.
torch>=2.0.0
numpy>=1.23.0
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
